% \section{Background: How a Linux System Call Works }

% In ARM64, application uses \texttt{svc \#0x0} to issue a syscall. Before the \texttt{svc} instruction, the application also needs to prepare the syscall number and parameters corresponding to the syscall.